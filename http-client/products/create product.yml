info:
  name: create product
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/products
  body:
    type: json
    data: |-
      {
        "name": "Iphone 12",
        "url": "https://elsitio.com",
        "site": "mercadolibre"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
